# Domain + IP Fusion Archive

This directory is a historical vNext/fusion design workspace. It captured the design discussion that led to the current vNext control surface, observability split, and Domain/IP policy model.

Current authority lives in:

- `docs/IMPLEMENTATION_ROADMAP.md`
- `docs/INTERFACE_SPECIFICATION.md`
- `docs/decisions/`
- `docs/testing/`
- Plane project `SNORT` for open work

Some files in this directory still use phrases like "single truth" inside their original historical context. Treat those claims as scoped to that archived design workspace, not as current repository authority.
